# 這裡列的檔案不會被上傳、也就不會被公開存取（語法同 .gitignore）。
# Vercel 時代是用 .vercelignore 做同一件事。
.git
.gitignore
.assetsignore
.wrangler
node_modules
.DS_Store
.env*
README.md
wrangler.jsonc
set-base-url.sh
tools/
docs/
assets/brand/contact-sheet.html
assets/brand/icons-sheet.html
